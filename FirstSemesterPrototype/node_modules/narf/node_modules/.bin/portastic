#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../portastic/bin/portastic" "$@"
  ret=$?
else 
  node  "$basedir/../portastic/bin/portastic" "$@"
  ret=$?
fi
exit $ret
